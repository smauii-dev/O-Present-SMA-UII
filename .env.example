# --------------------------------------------------------------------
# ENVIRONMENT
# --------------------------------------------------------------------
CI_ENVIRONMENT=production

# --------------------------------------------------------------------
# APP
# --------------------------------------------------------------------
app.baseURL = 'https://presensi.smauiiyk.sch.id/'
app.forceGlobalSecureRequests = true

# --------------------------------------------------------------------
# DATABASE - PostgreSQL (NeonDB or other)
# --------------------------------------------------------------------
database.default.hostname = your-hostname
database.default.database = your-database
database.default.username = your-username
database.default.password = your-password
database.default.DBDriver = Postgre
database.default.port = 5432
database.default.DBPrefix =
database.default.charset = utf8

# --------------------------------------------------------------------
# SESSION - DatabaseHandler (uses ci_sessions table)
# --------------------------------------------------------------------
app.sessionDriver = CodeIgniter\Session\Handlers\DatabaseHandler
app.sessionSavePath = ci_sessions
app.sessionMatchIP = false

# --------------------------------------------------------------------
# EMAIL - SMTP Configuration
# --------------------------------------------------------------------
email.fromEmail = no-reply@yourdomain.com
email.SMTPUser = your-smtp-username
email.SMTPPass = your-smtp-password
email.SMTPHost = smtp.yourdomain.com
email.SMTPPort = 465
email.SMTPCrypto = ssl

# --------------------------------------------------------------------
# CSRF
# --------------------------------------------------------------------
app.CSRFEnabled = true
app.CSRFTokenName = csrf_test_name
app.CSRFCookieName = csrf_cookie_name
app.CSRFExpire = 7200

# --------------------------------------------------------------------
# LOGGING
# --------------------------------------------------------------------
app.logThreshold = 1

# --------------------------------------------------------------------
# S3 / MinIO / RustFS (Object Storage)
# Config\S3 reads: S3_ENDPOINT_FORCE (preferred) or S3_ENDPOINT
# --------------------------------------------------------------------
# Dev compose DNS:  http://smauii-rustfs-dev:9000
# Prod compose DNS: http://smauii-rustfs:9000
S3_ENDPOINT_FORCE = http://smauii-rustfs-dev:9000
S3_ENDPOINT = http://smauii-rustfs-dev:9000

# Browser-facing URL (nginx proxy / public). Falls back to FORCE if empty.
S3_PUBLIC_ENDPOINT = http://smauii-rustfs-dev:9000

S3_REGION = us-east-1
S3_ACCESS_KEY = opresent-access
S3_SECRET_KEY = opresent-secret
S3_BUCKET = opresent
S3_USE_PATH_STYLE = true
S3_PRESIGNED_URL_EXPIRY = 3600


# SvelteKit / Vite frontend
# Backend CI4 origin used by:
#   - Vite dev proxy (/api → CI4)
#   - SSR hooks ($lib/server/api) when set as CI4_API_ORIGIN

CI4_API_ORIGIN=http://127.0.0.1:8100
PUBLIC_API_ORIGIN=http://127.0.0.1:8100
